export default function Home() {
  return (
    <main>
      <h1>Chapter 16 — DX Enhancements and MCP Usage</h1>
      <p>
        The interesting files in this example are not in <code>app/</code>.
        Look at <code>.husky/</code>, <code>dangerfile.ts</code>,
        <code> CLAUDE.md</code>, <code>.mcp.json</code> and
        <code> .github/workflows/test.yml</code>.
      </p>
    </main>
  )
}
